/* =============================================
   BVETTER — Account Profile JS
   File: js/account-profile.js
   Depends: nav.js (handles dropdown)

   This page is mostly static HTML.
   nav.js already handles toggleUserMenu().

   TODO backend:
   - On load: api.getProfile() to populate:
     hero name, avatar, stat numbers
   - On load: api.getMyActivity() to populate
     the Recent Activity list dynamically
   - On load: api.getMyReports() for stat counts
   ============================================= */

/* Page is ready. No additional JS needed for now.
   nav.js handles the user dropdown. */
